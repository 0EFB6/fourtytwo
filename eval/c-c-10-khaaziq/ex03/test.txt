When you run hexdump on a file, it will output a series of lines. Each line represents a block of 16 bytes from the file, and each byte is displayed as a two-digit hexadecimal number. The ASCII representation of the bytes is displayed on the right-hand side of the output, with non-printable characters represented as dots.

Here is an example output of the hexdump command:
