int	ft_convert_zero(int i, int number_len, char *number, char *buffer)
{
	if ((number_len >= 3)
		&& (number[i] != '0'
			|| number[i + 1] != '0'
			|| number[i + 2] != '0'))
		ft_write_zeros(number_len, buffer);
	i += 3;
	return (number_len);
}